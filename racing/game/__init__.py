"""Пакет игровой логики: протокол, физика, трасса, машины, бонусы, симуляция.

Модули пакета намеренно не импортируются здесь: сервер тянет только то,
что ему нужно (`from game import protocol`), а зеркальные клиентские файлы
лежат в static/js/ и обязаны совпадать построчно по логике.
"""
