# -*- coding: utf-8 -*-
"""Серверная часть гонок: HTTP + WebSocket, комнаты, обнаружение серверов.

Модули пакета намеренно не импортируются здесь, чтобы точка входа тянула
ровно то, что ей нужно, и импорт пакета ничего не поднимал сам по себе.

Состав:
  config     — все константы и таймауты;
  player     — игрок и его соединение;
  room       — комната, конечный автомат гонки, менеджер комнат;
  discovery  — UDP-маяк и список серверов в локальной сети;
  app        — Tornado Application, HTTP-хендлеры и WebSocket-хендлер.
"""
