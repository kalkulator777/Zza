# Не относится к игре

`ВелосипедДрайв.zip` лежал в корне репозитория до начала работы над гонками.
Внутри — другой проект: приложение магазина велосипедов на PyQt (товары,
заказы, вход, SQLite). К ZZA он отношения не имеет.

Перенесён сюда, чтобы не мешался рядом с `main.py`, и сохранён как есть —
удалять чужой файл без спроса неправильно.
